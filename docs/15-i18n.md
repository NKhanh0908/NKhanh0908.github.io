# i18n Strategy

## Supported Languages

| Code | Language | Default |
|---|---|---|
| `en` | English | ✅ Yes |
| `vi` | Vietnamese | No |

---

## Library

`next-intl`

Compatible with Next.js 15 App Router.

Supports Server Components natively.

---

## URL Structure

Locale is embedded in the URL path:

```
/en        → English homepage
/vi        → Vietnamese homepage

/en/...    → English pages
/vi/...    → Vietnamese pages
```

Default locale (`en`) does not redirect — `/` resolves to `/en`.

---

## Configuration

### `src/i18n/config.ts`

```ts
export const locales = ['en', 'vi'] as const;
export type Locale = (typeof locales)[number];
export const defaultLocale: Locale = 'en';
```

### `src/i18n/request.ts`

```ts
import { getRequestConfig } from 'next-intl/server';
import { locales, defaultLocale } from './config';

export default getRequestConfig(async ({ locale }) => {
  const resolvedLocale = locales.includes(locale as Locale)
    ? locale
    : defaultLocale;

  return {
    locale: resolvedLocale,
    messages: (await import(`./locales/${resolvedLocale}/common.json`)).default,
  };
});
```

### `middleware.ts` (root)

```ts
import createMiddleware from 'next-intl/middleware';
import { locales, defaultLocale } from '@/i18n/config';

export default createMiddleware({
  locales,
  defaultLocale,
});

export const config = {
  matcher: ['/((?!api|_next|.*\\..*).*)'],
};
```

---

## Translation File Structure

Each section has its own namespace file.

### `en/hero.json`

```json
{
  "greeting": "Hi, I'm Khanh.",
  "role": "Backend-focused Fullstack Developer",
  "description": "I enjoy building secure, scalable and maintainable systems using modern backend technologies.",
  "cta": {
    "projects": "View Projects",
    "contact": "Contact Me"
  }
}
```

### `vi/hero.json`

```json
{
  "greeting": "Xin chào, mình là Khánh.",
  "role": "Lập trình viên Fullstack hướng Backend",
  "description": "Mình thích xây dựng các hệ thống bảo mật, có thể mở rộng và dễ bảo trì bằng các công nghệ backend hiện đại.",
  "cta": {
    "projects": "Xem dự án",
    "contact": "Liên hệ"
  }
}
```

---

## Namespace Map

| File | Section |
|---|---|
| `common.json` | Navbar, Footer, shared labels, metadata |
| `hero.json` | Hero section |
| `about.json` | About / Where It Started |
| `experience.json` | Professional Experience |
| `skills.json` | Technologies I Work With |
| `projects.json` | Featured Projects |
| `contact.json` | Contact section |

---

## Usage in Components

### Server Component

```tsx
import { useTranslations } from 'next-intl';

export function HeroSection() {
  const t = useTranslations('hero');

  return (
    <section>
      <h1>{t('greeting')}</h1>
      <p>{t('role')}</p>
      <p>{t('description')}</p>
    </section>
  );
}
```

### Client Component

```tsx
'use client';
import { useTranslations } from 'next-intl';

export function ContactForm() {
  const t = useTranslations('contact');
  // ...
}
```

---

## Language Toggle

Location: Navbar — right side, next to Theme Toggle.

Display: Show current locale as text (`EN` / `VI`).

Behavior: Switches locale while preserving current path.

```tsx
import { useRouter, usePathname } from 'next/navigation';
import { locales } from '@/i18n/config';

export function LanguageToggle({ currentLocale }: { currentLocale: string }) {
  const router = useRouter();
  const pathname = usePathname();

  const switchLocale = (locale: string) => {
    // Replace current locale prefix with new one
    const newPath = pathname.replace(`/${currentLocale}`, `/${locale}`);
    router.push(newPath);
  };

  return (
    <div className="flex items-center gap-1">
      {locales.map((locale) => (
        <button
          key={locale}
          onClick={() => switchLocale(locale)}
          className={cn(
            'text-sm font-medium px-2 py-1 rounded-md transition-colors',
            locale === currentLocale
              ? 'text-accent'
              : 'text-secondary hover:text-primary'
          )}
          aria-label={`Switch to ${locale === 'en' ? 'English' : 'Tiếng Việt'}`}
          aria-current={locale === currentLocale ? 'true' : undefined}
        >
          {locale.toUpperCase()}
        </button>
      ))}
    </div>
  );
}
```

---

## SEO — hreflang

Add `alternates` to every page metadata:

```ts
export async function generateMetadata({ params }: { params: { locale: string } }) {
  return {
    alternates: {
      languages: {
        'en': '/en',
        'vi': '/vi',
      },
    },
  };
}
```

---

## Translation Maintenance Rules

- EN is the source of truth — write EN first, then translate to VI.
- Keep JSON keys identical across all locales.
- Never translate technical terms: Java, Spring Boot, WebSocket, Redis, JWT, API, RBAC...
- Project names stay unchanged across locales.
- Company names stay unchanged across locales.

---

## Content Notes — Vietnamese

| EN | VI |
|---|---|
| Backend-focused Fullstack Developer | Lập trình viên Fullstack hướng Backend |
| Real-time Communication Platform | Nền tảng giao tiếp thời gian thực |
| HRM System | Hệ thống quản lý nhân sự |
| View Projects | Xem dự án |
| Contact Me | Liên hệ |
| Where It Started | Bắt đầu từ đây |
| Technologies I Work With | Công nghệ mình sử dụng |
| Professional Experience | Kinh nghiệm làm việc |
| Let's Build Something Together | Hãy cùng xây dựng điều gì đó |